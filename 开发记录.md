# 开发迭代记录（Changelog）

本文件记录“一箭又一箭”从骨架到完成的关键迭代步骤。
每一步对应一次真实的代码修改与测试验证。

## v0.1 项目骨架
- 建立 `main.py` / `game.py` / `logic.py` / `levels.py` / `theme.py` / `graphics.py` / `effects.py` 文件结构。
- 搭建开始界面、游戏主界面、结算界面三个状态。
- 建立 `pygame` 主循环与状态机。

## v0.2 四方向路径检测
- 在 `logic.py` 实现 `is_blocked()`：方向统一映射为 `(dr, dc)` 向量，
  用 `while 0 <= r < rows and 0 <= c < cols` 逐格前进，越界自动退出。
- 四个方向共用一段代码，从根上避免数组越界。

## v0.3 棋盘与渐变箭头渲染
- `graphics.py` 用 4 倍超采样 + `smoothscale` 绘制平滑箭头。
- 箭头主体用纵向渐变，四方向各一套配色。
- 圆角面板、网格线、悬停高亮。

## v0.4 点击交互与飞出动画
- 点中可飞箭头：棋盘格子立即置空，同时生成 `FlyingArrow` 沿方向飞出。
- 飞出越飞越快，沿途拖火花粒子。

### fix：箭头飞行方向轴对调
- 初版把逻辑层 `(dr, dc)` 直接当屏幕坐标 `(x, y)`，导致上箭头往左飞。
- 修正为：x 方向用 `dc`、y 方向用 `dr`。

## v0.5 碰撞反馈与失误次数
- 被挡箭头：晃动变红、粒子爆裂、飘 `-1`。
- 心形显示剩余失误，点光判失败。

## v0.6 通关 / 失败结算与关卡切换
- 清空全部箭头 → 弹通关面板；失误点光 → 弹失败面板。
- 支持“下一关 / 重开 / 返回主页”。

### fix：第 1 关死锁
- 初版第 1 关 `RUL / ... / .D.` 中，`R` 与 `L` 正面对撞，互相阻挡，求解器判定无解。
- 用 `solve_level()` 重排关卡，全部关卡改为手工推导依赖链 + 求解器自动验证。

## v0.7 提示 / 撤销 / 自动求解
- **提示（H）**：复用 `solve_level()` 取第一步，呼吸光圈高亮。
- **撤销（Z）**：记录操作历史，退回箭头、恢复失误。
- **自动求解（A）**：按固定节奏逐步播放解法，可随时接管。

### fix：自动求解同帧消失
- 初版一个循环里点完所有箭头，导致同帧全消失、结算早于动画。
- 改为定时器每 `AUTO_STEP_DELAY` 秒推进一步。

## v0.8 星级评价与计时
- `logic.compute_stars()` 纯函数：按失误数、用时、是否用提示/自动求解给 1~3 星。
- 增加倒计时与可视化时间条，时间用尽判失败。

## v0.9 选关界面
- 开始游戏后先进入选关界面，四张卡片任选一关。
- 结算面板增加“选关”按钮，可自由跳关。

## v1.0 界面美化与收尾
- 开始/结算标题改渐变金属字，文字加投影。
- 失败原因区分“超时”与“失误耗尽”两种文案。
- 补充单元测试至 22 条，全部通过。

## v1.1 抽出 GameState：规则层与界面层分离
- 新增 `logic.GameState`：点击判定、失误扣减、胜负、撤销历史、重新开始
  全部收进这个不依赖 pygame 的类；`game.py` 只负责渲染和动画。
- 新增 `GameState.from_level()` / `from_rows()` / `snapshot()` / `stars_for()`。
- 新增 `logic.validate_level()`：把“关卡是否真的有解”做成可复用的校验函数。
- 收益：作业要求的 T01~T06 六个测试项从“手点”变成自动化测试。

### fix：结算面板里的星星和文字整体偏出面板
- 结算面板原先画在一张“面板尺寸 + 80”的离屏层上，再把这层居中贴到画布，
  于是层内坐标和画布坐标差了 40 像素，星星和文字全偏到面板外面（截图里可见）。
- 改为把面板画在与窗口同尺寸的层上，层内坐标即窗口坐标，不再需要换算。
- `_draw_star_row()` 改为显式接收目标 Surface（结算画面在 layer、选关画面在 canvas）。

### fix：用过提示/自动求解，星级上限不生效
- 现象：用了自动求解通关，结算照样给 3 星（应被压到 1 星）。
- 根因：`Game` 和 `GameState` **各存了一份** `hints_used` / `auto_used`，
  界面更新的是自己那份，而 `stars_for()` 读的是 `GameState` 那份（永远是 0）。
- 修法：只保留 `GameState` 一份状态，界面不再自己存计数。
- 发现方式：`self_test.py` 无头整局冒烟测试（单元测试覆盖不到这种跨层不同步）。

## v1.2 提示与自动求解的健壮性
- 自动求解不再开局排好整份解法队列，改为**每步重新求解当前局面**
  （解法按棋盘快照缓存）：玩家中途自己点掉箭头或用了撤销后，旧队列的坐标早已失效。
- 解法缓存从“手动 invalidate”改为按 `snapshot()` 自动失效，
  避免漏调导致提示指向已经清空的格子。

### fix：环境光尘导致渲染崩溃
- `effects.draw_ambient()` 引用了 `theme.AMBIENT_COLOR`，而该常量根本没定义过，
  因此这段代码从未被调用（一直是死代码），一旦接进渲染循环就会 AttributeError。
- 补上颜色常量，并把光尘接进 `Game.draw()`。

## v1.3 随机关卡（保证有解）
- 新增 `logic.directions_for_cell()`：列出某格“此刻就能直接飞出”的方向。
- 新增 `logic.generate_level()`：**逆序构造**——从空棋盘开始，只允许把箭头放在
  当前就能飞出的位置，放置顺序倒过来即合法通关顺序，因此构造性保证不会死锁；
  生成后再交给 `solve_level()` 独立复验一次。
- 三个难度档位（4×4 / 5×5 / 6×6），难度用箭头占格子的比例控制。
- 选关界面增加「随机·轻松 / 标准 / 困难」三个按钮。

### fix：第一版“公共出口十字骨架”本身有死锁
- 初版方案是选一个空格当公共出口，同行/同列的箭头都指向它，听起来很合理。
- 参数扫描发现大量组合生成失败；逐层排查后定位到根因：
  **出口行两侧若都放指向出口的箭头，会正面对撞互相挡住**——满格时必然死锁。
- 结论：改成逆序构造，并用 516 组（尺寸 × 箭头数 × 种子）扫描确认全部可解。

### fix：逆序构造的解法顺序被多反转了一次
- `solve_level()` 返回的本就是“从初始棋盘点到空棋盘”的顺序，
  与逆序构造的放置顺序恰好互为反序，代码里却又 `reverse()` 了一次，导致回放点不动。
- 用“按解法逐步点击、每步都必须真的能飞出”的回放测试抓出。

## v1.4 求解器性能：BFS → DFS
- 现象：6×6 / 29 个箭头的密集棋盘，BFS 求解要 43 秒，提示功能会直接卡死界面。
- 分析：本游戏每点一次必定消掉一个箭头，因此只要搜到空棋盘，
  路径长度必然等于箭头数——任意合法路径都不会浪费点击，
  BFS 唯一的优势（最短路径）在这里没有意义，却要付出展开全部状态的代价。
- 改为带记忆化的 DFS，同一棋盘降到 0.1 毫秒；保留 `max_states` 兜底防止界面卡死。

## v1.5 反馈与进度
- 碰撞反馈增加“朝自己的方向顶出去再弹回来”的位移（原先只有左右晃动），
  方向感更明确；粒子、飘 `-1`、心形扣减保留。
- 新增 `savegame.json` 进度存档：记录每关最好星级，选关界面用星星展示；
  存档损坏或写盘失败都静默忽略，不影响游戏运行。
- 新增背景漂浮光尘（原先实现了却没接进渲染循环）。

## v1.6 测试体系
- `test_logic.py`（33 条）：路径检测、边界、死锁校验、随机关卡生成、星级纯函数。
- `test_gameplay.py`（40 条）：T01~T06 六个必测项的自动化版本 + 撤销 + 星级。
- `self_test.py`：无头整局冒烟测试，像玩家一样把 4 关和扩展功能真的跑一遍。
- `make_screenshots.py`：无头导出 7 张界面截图（开始/选关/游戏/碰撞/通关/失败/随机）。
- 合计 73 条单元测试 + 1 个整局冒烟测试，全部通过。

## 自动化测试
```
python -m unittest test_logic.py test_gameplay.py -v
Ran 73 tests ... OK

python self_test.py
冒烟测试全部通过：界面、路径判断、碰撞、结算、扩展功能均正常
```
